packages:
  - 'apps/*'
  - 'packages/*'
  - 'api'

allowBuilds:
  esbuild: false

# pnpm 10 blocks dependency build scripts by default; allow the ones our
# toolchain genuinely needs to compile native binaries during install.
onlyBuiltDependencies:
  - esbuild
  - '@tailwindcss/oxide'
